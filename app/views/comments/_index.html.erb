<li>コミュニティガイドラインを遵守し、良識のあるコメントを心がけましょう</li>
<ul>
<table class="table table-hover">
  <thead>
    <tr>
      <th>コメントNo.</th>
      <th>コメント内容</th>
      <th>コメント者</th>
      <th>コメント投稿日時</th>
    </tr>
  </thead>

<tbody>

  <% comments.each.with_index(1) do |comment, i| %>
    <% if comment.id.present? %>
      <tr>
        <div class="comment-content" id ="this_comment_<%= comment.id %>"></div>
        <td><%= "#{i}"%></td>
        <td><%= comment.content %></td>
        <td><%= link_to comment.user.name,@users %></td>
        <td><%= comment.created_at.strftime("%Y-%m-%d %H:%M") %></td>

        <% if comment.user == current_user %>
        <td><%= link_to "編集", edit_community_comment_path(community, comment),remote: true %><i class="fas fa-edit"></i></td>
        <td><%= link_to "削除", community_comment_path(comment.community_id, comment.id),method: :delete, remote: true,
                                       data: { confirm: "本当に削除しますか?"} %><i class="fas fa-trash-alt"></i></td>

        <% end %>
      </tr>
    <% end %>
  <% end %>

</tbody>
</table>

</ul>
